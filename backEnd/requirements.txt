fastapi[all]
uvicorn[standard]
passlib[bcrypt]
# bcrypt fijo: passlib 1.7.4 rompe con bcrypt >= 4.1. En su rutina interna de
# deteccion de bugs le pasa una clave de mas de 72 bytes, y las versiones nuevas
# lanzan ValueError en vez de truncar. Sintoma: 500 en todo registro y login.
bcrypt==4.0.1
requests
python-multipart
# Pony 0.7.16 solo anda hasta Python 3.10: arma las consultas decompilando el
# bytecode de los lambda, y 3.11 lo cambio. Sintoma: IndexError en cualquier
# consulta, o sea 500 en registro y login. La 0.7.20 soporta hasta 3.14.
pony==0.7.20
email-validator==1.3.0
PyJWT==2.5.0
pytest==7.1.3
coverage==6.5.0